DROP TABLE IF EXISTS sum;
CREATE TABLE sum (
id INT AUTO_INCREMENT,
day DATE,
time INT,
contents VARCHAR(255),
lang VARCHAR(255),
PRIMARY KEY(id)

);

INSERT INTO sum (day, time, contents, lang) VALUES
(2020),
(),
();
